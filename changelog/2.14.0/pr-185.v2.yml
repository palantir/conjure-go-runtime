type: improvement
improvement:
  description: Refactor request retrier for performance and comprehension. Fixes off-by-one bug where the first retry (second attempt) sees no backoff.
  links:
  - https://github.com/palantir/conjure-go-runtime/pull/185
